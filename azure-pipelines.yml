trigger:
  branches:
    include:
    - master
    - refs/tags/*

variables:
  publish_repo: wb-artifacts
  python_version: 3.6

jobs:
  - job: CommitTest
    displayName: CI Commit Test
    pool:
        vmImage: 'ubuntu-16.04'
    steps:
      - task: UsePythonVersion@0
        displayName: Set Python version to $(python_version)
        inputs:
            versionSpec: "$(python_version)"

      - task: CmdLine@2
        displayName: Prepare virtualenv
        inputs:
            script: make init

      - task: CmdLine@2
        displayName: Run tests
        inputs:
            script: make pre-commit

      - task: PublishTestResults@2
        displayName: Publish tests
        condition: succeededOrFailed()
        inputs:
            testResultsFiles: "junit/test-*.xml"
            testRunTitle: "Publish test results for Python $(python.version)"

  - job: Publish
    displayName: Publish
    dependsOn: CommitTest
    condition: and(succeeded(), startsWith(variables['Build.SourceBranch'], 'refs/tags/'))
    steps:
      - task: CmdLine@2
        displayName: Package
        inputs:
            script: make package

      - task: TwineAuthenticate@0
        displayName: Authenticate with $(publish_repo) repo
        inputs:
            artifactFeeds: $(publish_repo)

      - task: CmdLine@2
        displayName: Publish to $(publish_repo)
        condition: and(succeeded(), startsWith(variables['Build.SourceBranch'], 'refs/tags/'))
        inputs:
            script: pipenv run twine upload -r $(publish_repo) --config-file $(PYPIRC_PATH) .tox/dist/*